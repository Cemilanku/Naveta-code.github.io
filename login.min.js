function masuk() {
			var user = document.getElementById("uname").value;
			var pass = document.getElementById("pass").value;
//ADMIN ACCOUNT
         if(user === "kopi" && pass === "1234"){
             alert ("Selamat Datang" + "user");
        }
//CUSTOM ACCOUNT
         else{
             alert ("Selamat Datang" + user);
        }
    }
